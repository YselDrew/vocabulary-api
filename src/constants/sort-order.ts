export enum SortOrder {
  ASC = 'ASC',
  DESC = 'DESC',
}

export const sortWordListMap = {
  word: 'word',
  translation: 'translation',
  createdAt: 'created_at',
};

export const DEFAULT_WORD_LIST_LIMIT = 25;
export const DEFAULT_WORD_LIST_OFFSET = 0;
